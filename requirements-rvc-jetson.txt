# RVC (Voice Conversion) Dependencies - NVIDIA Jetson (aarch64)
#
# This file is for Jetson devices (JetPack 6.x, CUDA 12.x, Python 3.10).
# PyTorch and torchaudio are EXCLUDED here because they must be installed
# separately from NVIDIA's Jetson-specific wheel index:
#
#   pip install torch torchaudio --index-url https://pypi.jetson-ai-lab.dev/jp6/cu126/
#
# The setup.sh and scripts/install_rvc.sh scripts handle this automatically.
#
# RVC on Jetson is EXPERIMENTAL - some packages (fairseq, rvc-python) may
# require building from source on aarch64.

# Core RVC library
rvc-python>=0.1.0

# Fairseq (required by RVC models) - may need to build from source on aarch64
fairseq>=0.12.0

# Audio processing for RVC
librosa>=0.10.0
resampy>=0.4.0

# Note: soundfile is already in main requirements.txt
