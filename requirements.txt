# Core Dependencies
openai>=1.12.0
numpy>=1.24.0
scipy>=1.11.0

# Audio Processing
pyaudio>=0.2.13
sounddevice>=0.4.6
soundfile>=0.12.1
webrtcvad>=2.0.10
pydub>=0.25.1

# RVC Voice Conversion (optional - for custom voice personalities)
# NOTE: RVC has complex dependencies. Install manually if needed:
#   pip install rvc-python
# The system will work without RVC (using OpenAI TTS voices only)

# Wake Word Detection
# OpenWakeWord (open source, flexible, easy custom wake word creation)
openwakeword>=0.5.0
onnxruntime>=1.16.0

# Picovoice Porcupine (optional alternative, requires API key)
# pvporcupine>=3.0.0

# Sentiment Analysis
vaderSentiment>=3.3.2
textblob>=0.17.1

# Syllable Detection (for lip sync)
syllables>=1.1.4
pyphen>=0.17.2

# Configuration Management
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Utilities
python-dateutil>=2.8.2
psutil>=5.9.0

# Development/Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0
